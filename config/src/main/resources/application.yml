server:
 port: 3333
eureka:
 client:
  service-url:
   defaultZone: http://peer1:1111/eureka/
 instance:
  prefer-ip-address: true
  instance-id: ${spring.cloud.client.ip-address}:${server.port}
spring:
 application:
  name: spring-cloud-config
 cloud:
  config:
   server:
    git:
     uri: https://github.com/SiShenMingMing/config.git
     username: sishenmingming
     password: 5564100.jwt
     searchPaths: '{profile}'
   label: master

